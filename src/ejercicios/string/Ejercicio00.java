package ejercicios.string;

public class Ejercicio00 {
    public static void main(String[] args) {

        // 1. Abre la documentación oficial de la clase String:
        // https://docs.oracle.com/javase/8/docs/api/java/lang/String.html
        // Baja hasta la sección Method Summary
        tring()


        // 2. Lee la lista de métodos públicos de la clase String.
        //    NO es necesario entenderlos todos, solo fíjate en:
        //    - qué nombre tienen
        //    - qué hacen (descripción corta)
        //    - qué tipo devuelven (return)
        String()
        Inicializa un objeto recién creado Stringde modo que represente una secuencia de caracteres vacía.
        String(byte[] bytes)
        Construye un nuevo Stringobjeto decodificando la matriz de bytes especificada utilizando el conjunto de caracteres predeterminado de la plataforma.
                String(byte[] bytes, Charset charset)
        Construye un nuevo Stringobjeto decodificando la matriz de bytes especificada utilizando el conjunto de caracteres especificado .
        // 3. Elige UN método de String que NO hayamos usado en clase.
        String(StringBuffer buffer)
        Asigna una nueva cadena que contiene la secuencia de caracteres que actualmente se encuentra en el argumento del búfer de cadena.
        // 4. Escribe aquí el nombre del método elegido (solo como comentario)
metodo buffer
        // 5. Explica con tus palabras qué hace ese método (solo un comentario, no código)
Asigna una cadena  que contiene la secuencia de caracteres
        // 6. Indica un ejemplo de uso del método (solo escribe qué resultado esperas, no hace falta que funcione)
El resultado que espero es  que la cadena contenga la secuencia de caracteres
        // IMPORTANTE:
        // - Este ejercicio NO es para ejecutar código
        // - Se evalúa que sepas buscar, leer y entender la API
        // - No copies la descripción literal de la web
    }
}